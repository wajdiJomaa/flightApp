{
  "name": "flightapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:clientLogin": "cypress run --reporter mocha-allure-reporter --headed",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report",
    "test:allure": "npm run cy:clientLogin || npm run report:allure"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.30.2",
    "allure-commandline": "^2.18.1",
    "cypress": "^10.6.0",
    "mocha-allure-reporter": "^1.4.0"
  }
}
